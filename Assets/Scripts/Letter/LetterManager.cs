using System;
using UnityEngine;

namespace Letter
{
    public class LetterManager : MonoBehaviour
    {
        public string pathData;

        private void Start()
        {
            //load data
            //parse data
            //create the DB of letters
        }
    }
}